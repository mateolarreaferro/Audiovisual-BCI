# Core dependencies
numpy>=1.21.0

# BrainFlow for signal processing
brainflow>=5.0.0

# Lab Streaming Layer for OpenBCI GUI connection
pylsl>=1.16.0

# Audio synthesis
pyaudio>=0.2.11

# Signal processing
scipy>=1.7.0

# Note: Standard library modules (time, math, threading, logging, glob, platform, collections)
# are built-in and don't need to be installed