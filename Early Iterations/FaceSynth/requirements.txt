# FaceMesh Synth Controller Requirements
# Python 3.10 or 3.11 recommended

# Core dependencies
opencv-python>=4.8.1.78
mediapipe>=0.10.14
numpy>=1.24.3

# Audio synthesis
pyaudio>=0.2.11

# Optional outputs
python-osc==1.8.3
mido==1.3.0
python-rtmidi==1.5.8

# Note: If mediapipe installation fails, try:
# pip install mediapipe==0.10.14
#
# For pyo installation issues on macOS:
# brew install portaudio
# pip install pyo
#
# For pyo on Windows:
# Download pre-built wheel from https://www.lfd.uci.edu/~gohlke/pythonlibs/
#
# For pyo on Linux:
# sudo apt-get install python3-dev libjack-jackd2-dev portaudio19-dev
# pip install pyo